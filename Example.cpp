#include "Example.h"
#define _USE_MATH_DEFINES
#include <math.h>
#include <cmath>
//static double B;
//static double A;
//static double C1;
//static double C2;

//double f(double x, double y, double t) { 
//	return C1 * t * pow(M_E, -((x - 7*A/15)*(x - 7 * A / 15) + (y - 8*B/15)*(y - 8 * B / 15)));
//}
//double u0(double x, double y) { return 0; }
//double g1(double x, double t) { return t * pow(M_E, x); }
//double g2(double y, double t) { return C2 * t * sin(M_PI * y / B); }
//double g3(double x, double t) { return 0; }
//double g4(double y, double t) { return C2 * t * sin(M_PI * y / B); }